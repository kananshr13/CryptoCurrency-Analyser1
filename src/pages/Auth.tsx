import React from "react";

const Auth: React.FC = () => {
  return (
    <div style={{ padding: 24 }}>
      <h2>Auth Page</h2>
      <p>Placeholder for authentication UI (login / signup)</p>
    </div>
  );
};

export default Auth;
